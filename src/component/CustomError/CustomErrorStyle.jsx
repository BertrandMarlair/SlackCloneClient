import { dangerColor } from '../../style/constant'

const CustomErrorStyle = {
    container: {
        color: dangerColor,
        padding: 20,
        borderRadius: 3,
        width: '100%',
    }
}

export default CustomErrorStyle